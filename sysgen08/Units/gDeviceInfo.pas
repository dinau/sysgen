unit gDeviceInfo;

{$IFDEF FPC}
  {$MODE Delphi}
{$ENDIF}

{
// declare a global instance of the TDeviceInfo class so it's
// available to all the other units
// DeviceInfo created in UnitMain
}

interface

uses cDeviceInfo;

var
   DeviceInfo: TDeviceInfo;
implementation
    { nothing here}
    
end.
